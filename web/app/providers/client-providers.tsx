"use client";

/**
 * Client-side providers wrapper
 * Add any client-side providers here (e.g., theme providers, state management)
 */
export function ClientProviders({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
